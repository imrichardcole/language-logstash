'use babel';

import LanguageLogstashView from '../lib/language-logstash-view';

describe('LanguageLogstashView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
